<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />
  <!-- 外部工作区：bash 进程需要访问用户选择的目录（Android 11+ 分区存储）。 -->
  <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

  <application
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:theme="@style/Theme.Dsh"
    android:usesCleartextTraffic="true"
    android:supportsRtl="true">

    <service
      android:name=".EngineService"
      android:exported="false"
      android:foregroundServiceType="dataSync" />

    <!-- Android 16 平板深度适配：
         - resizeableActivity=true：显式声明支持自由调整窗口大小（桌面窗口模式
           Desktop Windowing / 分屏 / 自由窗口），10 英寸以上平板可任意缩放；
         - configChanges 覆盖 density|smallestScreenSize|fontScale：桌面窗口
           模式下拖动窗口边缘改变尺寸/密度时 Activity 不重建，WebView 会话
           与引擎桥保持连续（避免重建导致 WebUI 重载、JS 桥丢失）；
         - enableOnBackInvokedCallback：Android 13+ 预测性返回手势，平板
           桌面窗口模式下返回动画与系统一致。 -->
    <activity
      android:name=".MainActivity"
      android:exported="true"
      android:resizeableActivity="true"
      android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|smallestScreenSize|fontScale"
      android:enableOnBackInvokedCallback="true"
      android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- 内置控制台：快照 bash 交互终端（引擎未运行时也可排查）。 -->
    <activity
      android:name=".ConsoleActivity"
      android:exported="false"
      android:resizeableActivity="true"
      android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|smallestScreenSize|fontScale"
      android:enableOnBackInvokedCallback="true"
      android:windowSoftInputMode="adjustResize" />
  </application>
</manifest>